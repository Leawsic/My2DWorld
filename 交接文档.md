# My2DWorld 项目交接文档

> 本文档供下一任 AI/开发者快速了解项目全貌。最后更新：2026-07-31

---

## 一、项目简介

**My2DWorld** 是一个使用 **Python + pygame** 开发的 2D 横版"我的世界"风格沙盒游戏。支持：

- 无限地形生成（自研噪声，无第三方依赖）
- 64 个方向的区块动态加载/卸载
- 视野裁剪（仅渲染屏幕内的方块）
- 缩放（滚轮）、平移（鼠标右键拖拽）
- 方块悬停高亮 + 名称显示
- 自定义鼠标光标
- 旁观模式（WASD 自由移动 + 滚轮缩放 + 右键拖拽）
- 主界面（登录/注册/设置/语言切换）
- 用户系统（SHA-256 + 盐值加密存储）
- 中英双语（Ctrl+L 切换）

---

## 二、技术栈

| 项 | 值 |
|---|---|
| 语言 | Python 3.13 |
| 渲染 | pygame 2.6.1 (SDL 2.28.4) |
| 第三方库 | 仅 pygame（无其他依赖） |
| 字体 | 霞鹜文楷 `fonts/LXGWWenKai-Regular.ttf` |
| 资源 | 36 张方块贴图（8×8）、GUI 光标、6 张主页背景 |

---

## 三、目录结构

```
My2DWorld/
├── main.py                  # 程序入口：主页 → 登录 → 游戏主循环
├── homepage.py              # 主界面（登录/注册/设置入口 + 默认账号弹窗）
├── settings.py              # 设置界面（全屏/语言/默认调试开关）
├── account.py               # 用户注册/登录 + SHA-256 密码加密
├── world.py                 # 无限地形生成 + 区块管理 + 视野渲染
├── player.py                # 玩家移动（WASD）
├── config/
│   └── basic.json           # 设置持久化（全屏/语言/默认调试）
├── translate/
│   ├── translate.json       # GUI 文本 + 方块中文/英文名（双语）
│   └── block.json           # 方块贴图名 → 回退颜色
├── fonts/
│   └── LXGWWenKai-Regular.ttf
├── image/
│   ├── block/               # 36 张 8×8 方块贴图（stone 是石头层贴图）
│   ├── gui/                 # mouse.png（默认光标）、mouse_right_spectator.png（右键光标）
│   ├── Homepage_background/ # 6 张主页背景（1~6.jpg）
│   ├── player/              # 玩家贴图（player_stand.png 等）
│   ├── translate/           # 翻译目录
│   ├── config/              # 配置目录
│   └── fonts/               # 字体目录
├── Account_and_password/    # 用户账号存储（每个用户一个 JSON，密码为哈希+盐）
├── logs/                    # 日志目录
├── saves/                   # 存档目录（暂无内容）
├── other_codes/             # 辅助脚本（print_tree.py 等，非游戏代码）
├── 交接文档.md              # 本文档
└── README.md
```

> ⚠️ 注意：`image/translate/`、`image/config/`、`image/fonts/` 是旧版遗留目录，**不要使用**。当前实际配置在项目根目录的 `config/` 和 `translate/`。

---

## 四、启动与运行

```bash
python main.py
```

启动流程：
1. 打开 **主页**（随机背景）
2. 首次启动自动创建默认账号 **steve / 1234asdf**（存储在 `Account_and_password/steve.json`，密码为 SHA-256+盐值哈希）
3. 输入账号密码 → 登录 / 注册
4. 账号密码均留空时点登录 → 弹窗询问是否用 **steve** 默认账号登录
5. 登录成功后进入游戏

---

## 五、操作控制

| 操作 | 按键/鼠标 |
|---|---|
| 移动 | WASD 或方向键 |
| 缩放视野 | 鼠标滚轮 |
| 平移视野 | 按住鼠标右键拖拽 |
| 方块悬停 | 鼠标移到方块上（白色边框 + 顶部名称） |
| 切换语言 | **Ctrl+L**（中/英） |
| 调试信息 | F3 |
| 全屏 | F11 |
| 暂停菜单 | ESC（继续/退出） |
| 退出 | 暂停菜单 → 退出 / 主页 ESC |

---

## 六、代码模块详解

### 1. main.py —— 程序入口

- `main()`：初始化窗口 → 调用 `homepage()` 获取 `(username, lang, settings, screen_width, screen_height)` → 调用 `start_game()`
- `start_game()`：游戏主循环
  - 键盘/鼠标事件（移动、缩放、平移、暂停、语言切换）
  - 渲染流程：天空色填充 → `world.render_blocks()`（方块） → 悬停边框 → 顶部方块名 → 自定义光标 → 右下角模式标签 → 欢迎信息（3 秒淡出） → 调试信息 → 暂停菜单
- `GameMode` 类：模式常量（当前仅 `SPECTATOR`）

**关键点**：
- 主页返回的 `screen_width`/`screen_height` 才是真实窗口尺寸（解决 Maximize 后渲染缩放错误）
- 自定义光标：`pygame.mouse.set_visible(False)` + 每帧把光标贴图 blit 在鼠标位置

### 2. world.py —— 地形与渲染（核心）

**地形生成**：
- `terrain_height(x)`：多层正弦叠加 + 值噪声（`smooth_noise`/`hash_noise` 手写），输出每个 x 列的**表面高度**
- `get_block_type(x, y, surface_y)`：方块类型分层
  - 底部 `y <= 2`：**基岩** bedrock
  - 顶部 `surface_y`：**草方块** grass_block_side
  - 草下 `grass_depth = 2 ~ 5`（基于 hash_noise 随机）：**草方块**
  - 下接 `DIRT_DEPTH = 15` 层：**泥土** dirt
  - 再往下：**石头层**（85% stone / 10% cobblestone / 5% mossy_cobblestone 随机混合）

**区块系统**：
- `Chunk`：16 列 × 地表到底部的列块，预生成所有方块存入 `_blocks` dict
- `World`：
  - `update_view(camera_x)`：以相机所在区块为中心，加载 ±`view_distance`(默认8) 个区块，卸载远处的
  - `render_blocks()`：视锥裁剪——只遍历与屏幕相交的 chunk，只渲染 x/y 在可见范围内的方块
  - 屏幕坐标：`sx = int((wx - camera_x) * block_size + cx_off)`（注意：全部取整对齐）

**重要常量**：
```python
CHUNK_SIZE = 16        # 每区块列数
GRASS_DEPTH = 3        # 遗留常量（未使用，实际 grass_depth 按 hash 随机）
DIRT_DEPTH = 15
BEDROCK_THICKNESS = 2
```

### 3. player.py —— 玩家

```python
Player(start_x, start_y)
  .speed = 1.35        # 方块/秒（原4.0的1/3）
  .update(keys, dt)    # WASD + 方向键，根据期望值设置 velocity
```
- 世界坐标 Y 轴**向上**，W 增大 Y（向上）；A 减小 X（向左）

### 4. homepage.py —— 主界面

- 随机背景 + 半透明遮罩 + 标题 + 账号/密码输入框 + 登录/注册/设置按钮
- `TextInput` 类：文本输入框（密码模式显示 `*`），光标 500ms 闪烁，`Tab` 切换焦点
- `Button` 类：可点击按钮（hover 高亮）
- `homepage()` 返回 `(username, lang, settings, screen_width, screen_height)`，登录失败显示错误消息
- **空输入点登录**：弹出模态框，"使用默认账户 steve 登录?" + 确认/取消

**注意**：`settings.py` 会 `from homepage import Button`，所以按钮/输入框类放必须留在 `homepage.py`

### 5. settings.py —— 设置界面

- 设置项：全屏、语言、默认开启调试
- 持久化到 `config/basic.json`
- `settings_screen()` 返回 `(settings, screen, screen_width, screen_height)`
- 全屏切换、尺寸变化会触发背景重缩放与窗口重建

### 6. account.py —— 用户系统

```python
ACCOUNT_DIR = "Account_and_password"    # 每个用户一个 json
DEFAULT_USERNAME = "steve"               # 默认账号
DEFAULT_PASSWORD = "1234asdf"

hash_password(password, salt=None) -> (hash_hex, salt_hex)
register(username, password) -> bool     # False = 已存在
login(username, password) -> bool        # False = 验证失败
user_exists(username) -> bool
init_default_account()                   # 创建默认 steve 账号
```

- 密码存储格式（`Account_and_password/steve.json`）：
```json
{
  "username": "steve",
  "password_hash": "<sha256(salt+password)>",
  "salt": "<random hex>"
}
```

---

## 七、配置文件详解

### translate/translate.json

双语结构，每个 key 含 `{"zh": "...", "en": "..."}`：
```json
{
  "language": "zh",          // 默认语言
  "languages": {"zh": {...}, "en": {...}},
  "blocks": {                 // 方块名翻译
    "grass_block_side": {"zh": "草方块", "en": "Grass Block"},
    ...
  },
  "gui": {                    // 界面文字
    "window_title", "mode_spectator", "controls_hint",
    "debug_*", "welcome",
    "homepage_title", "homepage_username", "homepage_password",
    "homepage_login", "homepage_register", "homepage_settings",
    "homepage_login_success/failed", "homepage_register_success/exists",
    "homepage_input_username/password", "homepage_password_short",
    "homepage_default_login", "homepage_confirm", "homepage_cancel",
    "settings_*", "pause_title/resume/quit"
  }
}
```

### translate/block.json

方块贴图名 → 回退颜色（贴图缺失时用纯色渲染）：
```json
{
  "grass_block_side": {"color": [87, 171, 60]},
  "dirt": {"color": [130, 96, 52]},
  "stone": {"color": [120, 120, 120]},
  ...
}
```

### config/basic.json

```json
{
  "fullscreen": false,
  "language": "zh",
  "debug_default": true
}
```

---

## 八、坐标系与渲染公式（易踩坑）

- **世界坐标**：X 向右增大；Y **向上**增大（y=1 为最底部基岩）
- **屏幕坐标**：X 向右增大；Y **向下**增大
- **相机**：`camera_x/camera_y` = 屏幕中心的**世界坐标**（玩家位置 + 平移偏移）
- **方块→屏幕**（world.py render_blocks）：
  ```python
  sx = int((wx - camera_x) * block_size + cx_off)    # cx_off = screen_width // 2
  sy_px = int((camera_y - wy) * block_size + cy_off)  # cy_off = screen_height // 2
  ```
- **鼠标→世界**（main.py hovered block）：
  ```python
  wx = int(math.floor(camera_x + (mx - cx_off) / block_size))   # X用 floor
  wy = int(math.ceil(camera_y - (my - cy_off) / block_size))    # Y用 ceil ⚠️
  ```
  > ⚠️ Y 轴反向计算必须用 `ceil`，用 `floor` 会偏下一格（History：这个 bug 修过）

---

## 九、贴图资源说明

| 目录 | 文件 | 用途 |
|---|---|---|
| `image/block/` | 36 张 8×8 PNG | 方块贴图（world.py 中 `STONE = "stone"`，石头层混合 stone/cobblestone/mossy_cobblestone） |
| `image/gui/` | mouse.png (30×30), mouse_right_spectator.png (32×32) | 鼠标光标 |
| `image/Homepage_background/` | 1~6.jpg | 主界面/设置界面随机背景 |
| `image/player/` | player_stand.png 等 7 张 | 玩家贴图（尚未在游戏中渲染） |
| `image/translate/` | block_translate.json, gui_translate.json | ❌ 废弃（旧版） |
| `image/config/` | （空） | ❌ 废弃（旧版） |
| `image/fonts/` | （空） | ❌ 废弃（旧版） |

---

## 十、已知问题 & 注意事项

1. **矿石贴图（iron_ore 等）目前未在游戏中生成** —— `world.py` 的 `get_stone_variant` 只返回 stone/cobblestone/mossy_cobblestone，矿石贴图虽被加载但未使用
2. **玩家贴图尚未渲染** —— `image/player/` 有贴图但代码里没有绘制玩家实体
3. **方块在石头层的比例**：`get_stone_variant()`（world.py）通过 `hash_noise(x*131 + y*2837)` 决定 85%/10%/5%
4. **暂停 (ESC)**：游戏暂停时显示原系统鼠标（`set_visible(True)`），恢复后再隐藏
5. **主页→游戏** 使用主页返回的**实际窗口尺寸**，不得使用硬编码 1024×768
6. **光标闪烁**：`TextInput.update()` 用 `pygame.time.get_ticks()` 500ms 闪烁，不要改回基于帧的 `dt`（会闪屏）

---

## 十一、历史 Bug 修复记录（重要）

1. **地形坑洼落差大** → 降低噪声振幅 `(30,15,6)→(12,6,2.5)`，删除随机相位
2. **X<0 不生成地形** → 删除 `render_blocks` 中 `max(0, ...)` 对 x 下限的截断
3. **上下移动反了** → W 应为 `velocity_y = +speed`（世界 Y 向上）
4. **窗口缩放** → `pygame.RESIZABLE` + `VIDEORESIZE` 事件 + F11 全屏
5. **石头贴图错用 iron_ore** → 改为 `stone` 并新增 cobblestone/mossy 混合
6. **悬停边框偏差** → 方块渲染坐标与边框都要 `int(...)`；且**鼠标→世界 Y 用 ceil**
7. **右键拖动弹回** → 松手时保留 `camera_offset`，不清零
8. **主页输入 L 冲突** → 改为 **Ctrl+L** 切换语言
9. **主页 Maximize 后游戏错位** → `homepage()` 返回实际 `screen_width/height`，供 `start_game` 使用
10. **密码类型警告** → `salt: str | None = None`

---

## 十二、常见任务指引

### 新增一个方块
1. 把贴图放入 `image/block/`（与贴图文件名一致）
2. 在 `world.py` 定义常量（如 `GOLD = "gold_block"`）
3. 在 `get_block_type` 或 `get_stone_variant` 中返回该常量
4. 在 `translate/block.json` 加回退色
5. 在 `translate/translate.json` 的 `blocks` 加双语名

### 新增界面文本
1. 在 `translate/translate.json` 的 `gui` 加 `{"zh": "...", "en": "..."}`
2. 使用 `t(trans, "key", lang)` 获取文本

### 新 AI 接手步骤
1. 通读本文档
2. `python main.py` 运行体验
3. 重点看 `main.py` 主循环、`world.py` 地形/渲染、`homepage.py` 界面流程
4. 修改后跑 `python main.py` 验证无报错