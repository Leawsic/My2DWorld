# My2DWorld 项目交接文档

> 本文档供下一任 AI/开发者快速了解项目全貌。最后更新：2026-08-01

---

## 一、项目简介

**My2DWorld** 是一个使用 **Python + pygame** 开发的 2D 横版"我的世界"风格沙盒游戏。支持：

- 无限地形生成（自研噪声，无第三方依赖）
- 64 个方向的区块动态加载/卸载
- 视野裁剪（仅渲染屏幕内的方块）
- 缩放（滚轮）、平移（右键拖拽）
- 方块悬停高亮 + 名称显示
- 自定义鼠标光标
- **双游戏模式**：旁观模式（自由飞行）+ 创造模式（重力/跳跃/飞行/方块破坏）
- **F3+F4 快捷键**：游戏内即时切换两种模式
- 玩家实体渲染（站立/移动动画贴图 + 刚体碰撞箱）
- 方块破坏粒子效果（基于方块贴图采样）
- **世界存档**（玩家位置 + 被破坏方块持久化）
- 主界面（登录/注册/设置/语言切换/世界管理）
- 用户系统（SHA-256 + 盐值加密存储）
- 中英双语（Ctrl+L 切换）
- 日志系统（按系统时间命名，密码加密记录）

---

## 二、技术栈

| 项 | 值 |
|---|---|
| 语言 | Python 3.13 |
| 渲染 | pygame 2.6.1 (SDL 2.28.4) |
| 第三方库 | 仅 pygame（无其他依赖） |
| 字体 | 霞鹜文楷 `fonts/LXGWWenKai-Regular.ttf` |
| 资源 | 36 张方块贴图（8×8）、GUI 光标、6 张主页背景、玩家贴图（64×64） |

---

## 三、目录结构

```
My2DWorld/
├── main.py                  # 程序入口 + 游戏主循环 + 存档系统
├── homepage.py              # 主界面（登录/注册/设置/退出）
├── world_menu.py            # 世界选择/创建/修改/删除 + 模式选择
├── settings.py              # 设置界面（全屏/语言/默认调试）
├── account.py               # 用户注册/登录 + SHA-256 密码加密
├── logger.py                # 日志系统（系统时间命名 + 密码加密记录）
├── runtime.py               # 集中运行时数据迁移/路径管理
├── world.py                 # 无限地形生成 + 区块管理 + 方块破坏记录
├── player.py                # 玩家实体（物理/碰撞/纹理/动画）
├── particles.py             # 方块破碎粒子系统
├── gamemodes/
│   ├── __init__.py          # 模式常量/基类/工厂函数
│   ├── spectator.py         # 旁观模式（自由飞行 + 不渲染玩家）
│   └── creative.py          # 创造模式（物理玩家 + 方块破坏 + 粒子）
├── run/                     # 运行时数据目录（运行时自动创建）
│   ├── accounts/            # 用户账号存储（JSON，密码哈希）
│   ├── worlds/              # 世界列表 + 世界存档
│   ├── logs/                # 日志文件
│   └── config/              # 配置
├── config/
│   └── basic.json           # 设置持久化（全屏/语言/默认调试）
├── translate/
│   ├── translate.json       # 双语 GUI 文本 + 方块名 + 模式名
│   └── block.json           # 方块贴图名 → 回退颜色
├── fonts/
│   └── LXGWWenKai-Regular.ttf
├── image/
│   ├── block/               # 36 张 8×8 方块贴图
│   ├── gui/                 # mouse.png / mouse_right_spectator.png
│   ├── Homepage_background/ # 6 张主页背景
│   └── player/steve/        # 玩家贴图（stand/1.png + move/1~4.png，64×64）
├── other_codes/             # 辅助脚本（非游戏代码）
├── 交接文档.md              # 本文档
└── README.md
```

> ⚠️ **注意**：
> - 旧版 `Account_and_password/`、`logs/` 等目录已迁移至 `run/`（首次运行时自动迁移）
> - `image/translate/`、`image/config/`、`image/fonts/` 为旧版遗留目录，**不要使用**
> - 游戏运行数据（账号/世界/日志/配置）统一在 `run/` 下

---

## 四、启动与运行

```bash
python main.py
```

启动流程：
1. 打开 **主页**（随机背景，登录/注册/设置/退出）
2. 首次启动自动创建默认账号 **steve / 1234asdf**
3. 登录成功 → **世界管理界面**
4. 在世界界面 **创建世界** 时选择模式（旁观/创造）
5. 选择已有世界或刚刚创建的世界 → 进入游戏
6. 游戏内 **F3+F4** 可随时切换旁观 ↔ 创造

---

## 五、操作控制

| 操作 | 按键/鼠标 |
|---|---|
| **旁观模式**：自由移动 | WASD 或方向键 |
| **创造模式**：左右移动 | A / D 或 ← / → |
| **创造模式**：跳跃（支持二段跳） | 空格（短按） |
| **创造模式**：切换飞行 | 长按空格 3 秒 |
| **创造模式**：飞行时上下 | W / S（或 ↑ / ↓） |
| **创造模式**：破坏方块 | 鼠标左键（按住连续破坏） |
| 缩放视野 | 鼠标滚轮 |
| 平移视野（仅旁观模式） | 按住鼠标右键拖拽 |
| 方块悬停 | 鼠标移到方块上（白色边框 + 名称） |
| 切换游戏模式 | **F3+F4** 或 Ctrl+F4 |
| 切换语言 | **Ctrl+L**（中/英） |
| 调试信息 | F3 |
| 全屏 | F11 |
| 暂停菜单 | ESC（继续/回到开始菜单/退出游戏） |

---

## 六、代码模块详解

### 1. main.py —— 程序入口 + 游戏主循环 + 存档

**主流程：**
- `main()`：初始化窗口 → `homepage()` 登录 → `world_menu()` 选世界/模式 → `start_game()`
- `start_game()` 主循环：
  - 事件处理（暂停/模式切换/缩放/全屏/语言）
  - 模式逻辑分发：`current_mode.update(...)` → `current_mode.render_particles(...)` → `current_mode.render_player(...)`
  - 世界渲染（方块 → 悬停高亮 → 粒子 → 玩家 → 光标 → 模式标签 → 调试 → 暂停菜单）

**存档系统（问题5的解决方案）：**
- `world_save_path(username, world_name)` → `run/worlds/{user}_{world}.json`
- `load_world_save()`：读取玩家位置 + 被破坏方块
- `save_world_state()`：游戏退出时保存
- 存档格式：
  ```json
  {
    "player_x": 10.8,
    "player_y": 54.0,
    "broken_blocks": [[9, 57], [-3, 12]]
  }
  ```

**F3/F4 调试冲突处理（问题3的解决方案）：**
- F3 设置 `pending_f3_toggle = True`（延迟标记）
- 若随后按 F4（组合键）→ 取消 F3 标记，执行模式切换
- 模式切换时重置 `camera_offset_x/y = 0`（解决切回创造模式视野偏移问题）

---

### 2. gamemodes/ —— 模式系统（核心架构）

| 文件 | 内容 |
|---|---|
| `__init__.py` | 常量 `SPECTATOR`/`CREATIVE`、`GameModeBase` 抽象基类、`create_mode()` 工厂 |
| `spectator.py` | `SpectatorMode`：WASD 全向自由移动（速度4.0），`render_player()` 为空（不显示玩家），无碰撞 |
| `creative.py` | `CreativeMode`：调用 `player.update()` 物理引擎，左键破坏方块（冷却8帧），粒子系统 |

**GameModeBase 接口**（所有模式必须实现）：
```python
class GameModeBase:
    def __init__(self, player, world): ...
    def handle_event(self, event) -> bool: ...    # 可选
    def update(self, dt, keys, mouse_buttons, mx, my, block_size, hovered=None): ...
    def render_player(self, screen, camera_x, camera_y, block_size): ...
    def render_particles(self, screen, camera_x, camera_y, block_size): ...
    def reset(self): ...
```

---

### 3. player.py —— 玩家实体

**物理常量：**
```python
WALK_SPEED = 1.8        # 走路速度（格/秒）
FLY_SPEED = 3.5         # 飞行速度（格/秒）
JUMP_VELOCITY = 9.5     # 跳跃初速度（格/秒）
MAX_JUMPS = 2           # 二段跳
GRAVITY = 14.0          # 重力加速度（格/秒²）
FLY_HOLD_TIME = 3.0     # 长按空格切换飞行阈值（秒）
```

**碰撞箱（重要，已按反馈修正）：**
```python
BODY_HALF_WIDTH = 0.25   # 半宽 → 总宽 0.5 格
BODY_HALF_HEIGHT = 0.95  # 半高 → 总高 1.9 格
BODY_HEIGHT = 1.9
BODY_FOOT_OFFSET = 0.95
```
- `get_collision_rect()` 返回 `(left, bottom, width, height)`，中心 = 玩家坐标 = 贴图画面正中心
- `render()` 将 64×64 贴图**直接拉伸**到碰撞箱尺寸（0.5×1.9格），float 坐标 blit 避免像素偏移 → **贴图中心=碰撞箱中心=玩家位置**三者重合

**物理引擎：**
- `update(keys, dt, world)`：dt 归一化（60fps=1.0），内部转真实秒 `dt_sec = dt / 60.0`
- `_handle_fly_toggle()`：长按空格 3 秒切换飞行
- `_move_and_collide_x/y()`：X/Y 轴独立碰撞检测（脚底/头顶/左右墙）
- `_update_animation()`：**仅水平移动时播 move 动画**（飞行时上下移动用 stand 帧）

---

### 4. world.py —— 地形与渲染（核心）

**地形生成：**
- `terrain_height(x)`：多层正弦叠加 + 值噪声，输出表面高度
- `get_block_type(x, y, surface_y)`：基岩→草方块→泥土→石头层（含圆石/苔石混合）

**区块系统：**
- `Chunk`：16 列 × 地表到底部，预生成 `_blocks` dict
- `World`：- `update_view(camera_x)` 加载±8区块，卸载远处
- `render_blocks()`：视锥裁剪渲染

**方块破坏与存档（问题5）：**
- `World.broken_blocks`：`set[tuple[int, int]]` 记录所有被破坏方块
- `break_block(x, y)`：移除指定方块 + 记入 `broken_blocks`
- `_load_chunk()`：新区块生成时自动重新应用 `broken_blocks`（跨区块持久）
- `apply_broken_blocks(broken_list)`：从存档恢复破坏状态

---

### 5. world_menu.py —— 世界管理界面

- 世界列表：创建/进入/重命名/删除
- **创建世界时选择模式**：旁观（spectator）/ 创造（creative）两个高亮切换按钮
- 世界数据格式：`{"name": "...", "mode": "spectator"|"creative", "created_at": "..."}`
- 返回格式：`(world_name, mode_name, screen, width, height)` 或 `"back"` / `None`

---

### 6. particles.py —— 粒子系统

- `ParticleSystem.spawn(wx, wy, block_type, textures)`：生成 8 个粒子
- 粒子从**方块贴图本身**切割出小块（`subsurface`），重力+空气阻力+渐隐
- `ParticleSystem.update/render()`：物理与绘制

---

### 7. runtime.py —— 运行时数据管理

- 定义 `run/` 目录：accounts/worlds/logs/config
- `ensure_runtime_data()`：首次运行自动迁移旧目录数据

---

### 8. account.py + logger.py —— 账号与日志

**account.py：**
- `ACCOUNT_DIR = run/accounts`（经 runtime 迁移）
- `hash_password(password, salt=None)`：SHA-256 + 16字节盐
- `register/login/user_exists/init_default_account`
- 密码仅存哈希+盐（`run/accounts/steve.json`）

**logger.py：**
- `init_log()`：创建 `run/logs/YYYY-MM-DD_HH-MM-SS.log`
- `log_event()`：带时间戳记录
- `log_login/log_register`：**密码仅记录哈希+盐，不存明文**
- `log_game_start/log_game_end/log_pause/log_resume`：游戏事件

---

## 七、配置文件详解

### translate/translate.json

双语结构，每个 key 含 `{"zh": "...", "en": "..."}`：
- `mode_spectator` / `mode_creative`：模式名
- `controls_hint`：操作提示（创造模式专用）
- `world_mode` / `world_mode_spectator` / `world_mode_creative`：世界界面模式选择
- `pause_*`：暂停菜单
- `homepage_*` / `settings_*` / `world_*`：界面文本

### translate/block.json

方块贴图名 → 回退颜色。

### config/basic.json

```json
{
  "fullscreen": false,
  "language": "zh",
  "debug_default": true
}
```

---

## 八、坐标系与渲染公式（易踩坑）

- **世界坐标**：X 向右增大；Y **向上**增大（y=1 为最底部基岩）
- **屏幕坐标**：X 向右；Y 向下
- **相机**：`camera_x/camera_y` = 屏幕中心的**世界坐标**（玩家位置 + 平移偏移）
- **方块→屏幕**（world.py render_blocks）：
  ```python
  sx = int((wx - camera_x) * block_size + cx_off)
  sy_px = int((camera_y - wy) * block_size + cy_off)
  ```
- **鼠标→世界**（main.py hovered block）：
  ```python
  wx = int(math.floor(camera_x + (mx - cx_off) / block_size))
  wy = int(math.ceil(camera_y - (my - cy_off) / block_size))   # Y 必须用 ceil ⚠️
  ```

---

## 九、贴图资源说明

| 目录 | 文件 | 用途 |
|---|---|---|
| `image/block/` | 36 张 8×8 PNG | 方块贴图 |
| `image/gui/` | mouse.png, mouse_right_spectator.png | 鼠标光标 |
| `image/Homepage_background/` | 1~6.jpg | 主页/设置背景 |
| `image/player/steve/stand/1.png` | 64×64 | 玩家站立帧 |
| `image/player/steve/move/1~4.png` | 64×64 | 玩家移动动画（4帧循环） |

---

## 十、已知问题 & 注意事项

1. **创造模式玩家碰撞箱为细长型**（0.5×1.9格），贴图拉伸会显得瘦——这是按碰撞箱尺寸直接拉伸的结果，符合需求（碰撞箱0.5宽2格高，后按反馈改1.9格高）
2. **玩家贴图是64×64正方形**，`render()` 直接拉伸到碰撞箱尺寸（0.5×1.9格），不做宽高比保留
3. **创造模式无死亡/掉落**：坠深只记录坐标，无虚空重置（存档数据中 y 可能为很大的负数）
4. **旁观模式相机偏移在切回创造时被重置**：F3+F4 切换模式时 `camera_offset_x/y = 0`
5. **存档文件会不断增长**：`broken_blocks` 随时间累积，未做清理
6. **方块破坏在旁观模式下无效**：只有 CreativeMode 才调用 `world.break_block()`
7. **粒子系统在模式切回旁观时保留**：`create_mode()` 创建新模式时粒子系统重置
8. **F3 调试切换是延迟执行**：F3 先标记、下一帧生效，F3+F4 组合可取消（详见 main.py）

---

## 十一、历史 Bug 修复记录（重要）

1. **地形坑洼落差大** → 降低噪声振幅 `(30,15,6)→(12,6,2.5)`
2. **X<0 不生成地形** → 删除 `max(0, ...)` 下限截断
3. **上下移动反了** → W 应为 `velocity_y = +speed`
4. **窗口缩放** → `pygame.RESIZABLE` + `VIDEORESIZE`
5. **石头贴图错用 iron_ore** → 改为 stone + 圆石/苔石混合
6. **悬停边框偏差** → 全部 `int()`；鼠标→世界 Y 用 ceil
7. **右键拖动弹回** → 松手保留 camera_offset
8. **主页输入 L 冲突** → Ctrl+L
9. **主页 Maximize 错位** → homepage 返回实际宽高
10. **密码类型警告** → `salt: str | None = None`
11. **旁观者模式逻辑未分离** → 新建 `gamemodes/` 包（spectator.py + creative.py）
12. **创造模式无物理** → player.py 重写：重力/跳跃/二段跳/飞行/碰撞
13. **右键在创造模式也可平移** → 右键拖拽限定旁观模式
14. **重力数值过大** → `GRAVITY` 从 18 降至 14，且统一用真实秒 `dt/60`
15. **碰撞检测穿透** → 重写 `_move_and_collide_x/y` 范围公式
16. **上下移动播 move 动画** → 仅水平速度触发 move 帧
17. **F3+F4 同时关调试** → 延迟 F3 标记 + 组合键取消
18. **模式切换视野偏移** → 切换时重置 camera_offset
19. **无存档** → 玩家位置 + 破坏方块写入 `run/worlds/`
20. **碰撞箱中心错位** → 贴图拉伸到碰撞箱尺寸 + 浮点坐标 blit，中心精确重合

---

## 十二、常见任务指引

### 新增一个游戏模式
1. 在 `gamemodes/` 新建 `xxxx.py`，继承 `GameModeBase`
2. 在 `gamemodes/__init__.py` 添加常量 `XXXX` 和工厂分支
3. 在 `world_menu.py` 模式选择中加对应按钮
4. 在 `translate/translate.json` 加模式名翻译
5. 在 `main.py` 模式切换列表 `MODE_LIST` 加入

### 新增一个方块
1. 贴图放入 `image/block/`
2. `world.py` 定义常量并在 `get_block_type`/`get_stone_variant` 返回
3. `translate/block.json` 加回退色
4. `translate/translate.json` 的 `blocks` 加双语名

### 新增界面文本
1. `translate/translate.json` 的 `gui` 加 `{"zh": "...", "en": "..."}`
2. 使用 `t(trans, "key", lang)` 获取

---

## 十三、新 AI 接手步骤

1. 通读本文档
2. `python main.py` 运行体验（登录 steve/1234asdf → 创建/选择世界）
3. 重点看 `main.py` 主循环、`gamemodes/` 模式系统、`player.py` 物理
4. 修改后跑 `python main.py` 验证无报错
5. 如需重置游戏数据，删除 `run/` 目录（首次启动会重新迁移/创建）